import { CutBlogDescPipe } from './cut-blog-desc.pipe';

describe('CutBlogDescPipe', () => {
  it('create an instance', () => {
    const pipe = new CutBlogDescPipe();
    expect(pipe).toBeTruthy();
  });
});
